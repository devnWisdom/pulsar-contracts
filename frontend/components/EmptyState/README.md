Empty State UI components

Files:
- `EmptyState.tsx` — generic empty state with illustration, message and CTA
- `EmptyList.tsx` — small wrapper for list-style empty states

These components are lightweight, accessible, and intended to be imported into
the frontend application. They intentionally have no runtime dependencies and
are framework-agnostic beyond React.
